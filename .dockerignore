node_modules
.next
.git
.gitignore
Dockerfile
.dockerignore
docker-compose*.yml
npm-debug.log*

# env / secrets — pass these at runtime, never bake into the image
.env
.env.*

# guest uploads saved at runtime
uploads

# design mockups, not part of the Next build
wedding

# os cruft
.DS_Store
**/.DS_Store
coverage
*.tsbuildinfo
